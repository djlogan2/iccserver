describe.skip("Game counts and ratings", function() {
  it("Finishing an unrated game in a category increments correctly (total+1, unrated+1, rated+0)", function() {});
  it("Finishing an unrated game in a category does not change the rating", function() {});
  it("Finishing a rated game in a category increments correctly (total+1, unrated+0, rated+1)", function() {});
  it("Finishing an unrated game in a category does change the rating", function() {});
});
